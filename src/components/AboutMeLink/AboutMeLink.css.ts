import { sprinkles } from '@css/sprinkles.css'

export const aboutMeLink = sprinkles({
  display: 'inline-flex',
  alignItems: 'center',
})

export const arrowIcon = sprinkles({
  display: 'inline-block',
  ml: 2,
})

export const arrowIconPath = sprinkles({
  stroke: 'brand50',
})
